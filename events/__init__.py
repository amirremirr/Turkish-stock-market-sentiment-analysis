"""Candidate-event grouping over scored headlines.

Nothing in this package produces a *verified* real-world event. Groups are
formed by transparent rules over normalized entities, signal family, time
proximity and title similarity, and are called candidate events or algorithmic
event groups throughout. Calling them verified would claim a level of
resolution the method does not have.
"""

# Test-to-Risk Map

**Reviewed:** 2026-08-01 after Stage 4

This map reports behavioral evidence, not a test-count badge. "Covered" means deterministic regression fixtures protect the implemented contract named here. "Partial" means useful tests exist but an important branch, integration boundary, or historical condition remains outside the fixture. A passing suite does not validate the research conclusion.

## Stage 2 processing and preservation risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Omitted LLM item fabricated as neutral | **Covered** | `tests/test_llm_partial.py::test_score_partial_keeps_omitted_item_absent`, `::test_analyze_partial_preserves_only_explicit_valid_results`, and `tests/test_stage2_pipeline.py::test_omissions_exhaust_retry_limit_without_becoming_neutral` assert absent IDs remain absent/NULL and exhaust as failed. | Hosted APIs can introduce new response shapes; malformed envelopes are tested, but live schema drift is not. |
| Explicit neutral confused with missing | **Covered** | `test_duplicate_and_out_of_range_score_ids_are_never_fabricated`, `test_incomplete_batch_retries_only_missing_and_preserves_explicit_neutral`, and `tests/test_signal_variants.py::test_incomplete_scores_are_excluded_but_explicit_neutral_is_included` distinguish returned zero-score neutral from absence through storage and aggregation. | None for the tested contract; human disagreement about whether a headline should be neutral remains a measurement limitation. |
| Duplicate, out-of-range, or malformed result IDs | **Covered** | `tests/test_llm_partial.py` invalidates duplicate IDs, ignores out-of-range IDs, preserves only unique valid results, and raises on malformed structured output. | A live provider-side schema change still needs an integration check. |
| Missing-only scoring retry and exhaustion | **Covered** | `tests/test_stage2_pipeline.py::test_incomplete_batch_retries_only_missing_and_preserves_explicit_neutral` verifies only omitted items are retried; `::test_omissions_exhaust_retry_limit_without_becoming_neutral` and `tests/test_database_integrity.py::test_scoring_failures_exhaust_and_preserve_nulls` verify attempt counts, retry/failed transitions, idempotent exhausted state, errors, and NULL preservation. | The pipeline branch that records a raised scorer exception for every active candidate is not separately asserted end to end. |
| Total scoring loss mislabeled as degraded | **Covered** | `test_omissions_exhaust_retry_limit_without_becoming_neutral` requires a failed outcome when zero candidates score, while `test_run_all_fails_when_every_scoring_candidate_exhausts` verifies failed run/component persistence and structured `scoring_unavailable` diagnostics. Partial success remains degraded. | A transport exception uses a separate fail-fast branch; item-state persistence is covered, but every provider exception class is not simulated. |
| HTTP retry configuration/backoff | **Partial** | `tests/test_llm_partial.py::test_http_retry_limit_and_backoff_are_configurable_without_sleep` simulates two HTTP 503 responses followed by success and asserts configurable retry count and exponential delays. | Permanent HTTP exhaustion, timeout classes, and provider rate-limit headers are not separately simulated. |
| A pending/failed/excluded row enters aggregates | **Covered** | `tests/test_stage2_pipeline.py::test_failed_and_excluded_rows_are_omitted_from_aggregation` seeds explicit neutral, failed, and excluded rows and proves only the complete scored neutral participates. `test_omissions_exhaust_retry_limit_without_becoming_neutral` protects NULL failed output. `tests/test_experiment_policy.py::test_excluded_experiment_is_not_eligible_for_aggregation` also verifies an excluded experiment identity does not enter the policy set. | None for the tested eligibility contract. |
| Raw observation lost at the relevance filter | **Covered for current fetched-window behavior** | `tests/test_raw_ingestion.py` proves RSS and HTML return off-topic observations with rule/version metadata. `tests/test_database_integrity.py::test_raw_observations_survive_global_url_dedup_without_rerun_bloat` proves source observations are stored before canonical URL collapse and replay is idempotent. | Items never fetched because they fall outside the configured lookback, and items discarded/deleted before this schema existed, cannot be recovered by a test or migration. |
| Cross-source observation incorrectly deduplicated | **Covered for the documented two-layer policy** | `test_rss_preserves_identical_headlines_from_distinct_sources` keeps both source observations; `test_rss_deduplicates_stable_identity_within_one_source` suppresses replay; database integrity tests keep different-URL same-title canonical rows and same-URL source audit rows. | The canonical table still intentionally collapses a globally identical URL. Event-level story resolution is not covered by these headline tests. |
| Exclusion silently deletes or cannot be restored | **Covered** | `tests/test_database_integrity.py::test_exclusions_are_idempotent_restorable_and_keep_history` verifies one active row, timestamped restoration, re-exclusion history, and retained headline/raw rows. `test_permanent_delete_requires_explicit_confirmation` verifies the guard and raw-observation survival after confirmed canonical purge. | No CLI permanent purge exists by design; recovery of pre-audit historical deletion depends on backups. |
| Relevance-rule restoration removes another rule | **Covered for below/above examples; boundary partial** | `test_relevance_rule_exclusion_is_reversible_without_restoring_other_rules` verifies low-grade exclusion, idempotence, later restoration, and preservation of a manual exclusion. | Exact values just below, at, and above `0.25`, plus NULL relevance, are not all exercised together in a database-level boundary fixture. |
| Restored low-relevance decision is immediately recreated | **Covered** | `tests/test_stage2_pipeline.py::test_restored_llm_relevance_exclusion_survives_aggregate_reconciliation` verifies an explicit restoration survives aggregation and enters the baseline; a newly stored relevance judgment then supersedes the override and creates a fresh auditable decision. | Restoration is headline-wide because the current canonical scoring unit is a headline; later event resolution may require a narrower override policy. |
| Additive migration corrupts legacy rows | **Covered for the production-shaped fixture** | `tests/test_database_integrity.py::test_legacy_backfill_classifies_rows_once_without_rewriting_scores` checks complete/empty/partial classification. `::test_production_legacy_migration_is_additive_idempotent_and_nonregenerating` migrates a production-style legacy schema twice and asserts preserved table counts, exact scores/labels, unchanged sentinel derived rows, empty newly created research tables, and NULL historical experiment provenance. | Interrupted migration/rollback and every historical schema permutation are not simulated. |
| Component-kind semantics lost | **Covered at persistence boundary** | `test_scoring_success_and_legacy_batch_shapes_set_component_metadata`, LLM partial-analysis tests, and existing tuple-contract tests distinguish LLM synthetic compatibility, XLM-R softmax, and caller-supplied/legacy kinds. | No automated prose linter prevents a future report from using the word "probability" incorrectly; documentation review remains necessary. |

## Stage 2 run-health and external-dependency risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Final/component state or JSON diagnostics not persisted | **Covered for full success and named failure branches** | Database integrity tests check component columns, structured JSON, legacy mapping, and interrupted recovery. `tests/test_stage2_pipeline.py::test_run_all_full_success_persists_every_component` executes every component through mocked provider boundaries; `::test_run_all_full_ingestion_failure_persists_failed_and_skipped_states` and the scoring-exhaustion test verify failed/skipped persistence. Experiment-policy tests cover default failure and override degradation. | Every possible exception site and provider exception class is not independently injected. |
| Cached market data masks outage/staleness | **Covered for the decision helper** | `test_market_download_failure_distinguishes_fresh_cache_from_stale` proves a fresh cache degrades with `fresh_cache_used`, while absent/stale cache fails with `market_data_stale`. | Boundary age exactly at/over the configured day threshold and an actual yfinance exception through the full command are not separately tested. |
| Optional factor failure is treated as success | **Covered for degraded persistence** | `test_run_all_persists_degraded_component_states` injects an external-factor warning and verifies final and market-data component degradation plus structured diagnostics. | Individual EEM/oil/FX provider branches are not each simulated. |
| Partial or total RSS-source failure misclassified | **Covered through orchestration for total failure** | Step tests inject partial and total RSS failures, including exhausted HTML fallback. `test_run_all_full_ingestion_failure_persists_failed_and_skipped_states` carries total failure through `run_all()` persistence. | Partial source degradation is not repeated through a live-provider full run. |
| Configured optional FX failure mislabeled intentional skip | **Covered** | `tests/test_stage2_pipeline.py::test_fx_absence_is_skipped_but_configured_provider_failure_degrades` distinguishes absent configuration from a failing configured provider and verifies structured warning codes. | Rate-limit and empty-payload responses share the same structured decision family but are not separately fixture-tested. |
| Invalid complete `scored` row passes audit | **Code contract, no direct focused regression** | `_processing_audit` checks every required score/component/provenance field before reporting success. Aggregate tests also require complete fields. | Seed a structurally incomplete row marked `scored` and assert audit/run failure. |

## Stage 3 timing and signal risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Timezone conversion changes hour but not date | **Covered** | `tests/test_trading_calendar.py::test_aware_timestamp_is_normalized_before_date_and_bucket_assignment` and `tests/test_session_signal_storage.py::test_timezone_normalization_moves_date_and_hour_together` assert Istanbul date/hour conversion together. Naive-Istanbul interpretation is separately tested. | More provider-specific non-ISO formats are covered only by legacy parser tests, not all the way through session storage. |
| Regular session boundary is off by one | **Covered** | Parameterized `test_regular_session_boundaries` asserts 09:59:59, 10:00:00, 18:10:00, and 18:10:01 bucket/session behavior. | Exchange microstructure changes require a new rule version and fixtures. |
| Weekend/full holiday/half-day/consecutive closure wrong | **Covered for configured 2026 fixtures** | `tests/test_trading_calendar.py` covers weekend, full-day holiday, Ramazan half-day plus consecutive closure, Kurban half-day, Republic Day eve half-day, and next-session roll. | The calendar is configuration, not an exchange-calendar API; future years require authoritative maintenance and new tests. |
| Unknown time creates look-ahead | **Covered** | Parameterized unknown-time tests cover weekday, Friday, and half-day dates; holiday unknown time has its own bucket assertion. The compatibility wrapper is also tested. | Historical rows with an incorrect source date cannot be repaired from an absent timestamp. |
| Timestamp/bucket/rule version not stored | **Covered** | `tests/test_session_signal_storage.py::test_insert_stores_timestamp_bucket_and_first_reactable_session` checks canonical timestamp, bucket, signal date, rule version, and raw-observation timing fields. | Session-rule backfill over a real historical database is not separately snapshot-tested. |
| Weighted formula silently replaces simple baseline | **Covered** | `tests/test_signal_variants.py::test_exact_signal_variants_and_audit_fields` asserts exact arithmetic for all four variants, counts, shares, dispersion, source/event counts, and denominators. `tests/test_session_signal_storage.py::test_aggregate_stores_simple_baseline_and_weighted_sensitivities` verifies database persistence. | None for the tested formula; specification choice remains a research-design risk. |
| Zero/missing/invalid weights silently fall back | **Covered** | Signal-variant tests cover explicit neutral versus incomplete scores, empty input, zero denominators returning NULL, NULL relevance fallback, NaN identifiers, optional source/category maps, legacy time weighting, and rejection of invalid weights. | Pipeline calls currently use neutral source/category multipliers; future non-neutral mappings need their own configuration-level tests. |
| Source/event breadth lost in aggregate metadata | **Partial** | Exact variant tests check distinct source/event-record counts; session storage checks source breadth for distinct canonical rows; raw-ingestion tests separately prove cross-source observations survive a shared canonical URL. | No focused aggregate fixture yet proves that the pipeline unions shared-URL raw sources into `source_count`. `event_count` is bridge-linked record breadth, not independently resolved event coverage. |
| Multiple experiment identities silently mixed | **Covered** | `tests/test_experiment_policy.py` verifies detection, default blocking before derived-row replacement, exclusion-aware eligibility, explicit override, CLI opt-in, and persisted failed/degraded run states. The production migration fixture proves historical rows stay visibly unassigned. | Experiment IDs still depend on correct configuration and operator discipline; the explicit override deliberately permits a degraded mixed result. |
| Derived rows remain stale after eligibility change | **Partial** | Legacy `tests/test_pipeline.py::TestAggregation::test_aggregate_clears_stale_rows` verifies orphaned-day removal. Exclusion aggregation and session-storage tests verify current rebuild behavior. | No single regression seeds stale rows in all five derived tables and verifies all are removed together. |

## Stage 3 predictive-alignment risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Predictive consumer falls back to calendar aggregate | **Covered for named consumers** | `tests/test_predictive_consumer_alignment.py` checks `visualize`, `dashboard`, `explore_signal`, and `analyze_external`; its external fixture deliberately omits `daily_sentiment`. `tests/test_evaluate_session_signals.py` verifies `evaluate` labels `simple_mean` primary and legacy category output descriptive. | A new future predictive module must be added to this contract explicitly. |
| Next-session return shifted after sparse join | **Covered** | `test_visualize_uses_variant_baseline_and_complete_price_lead`, `test_explore_targets_are_formed_before_signal_filtering`, `test_external_loader_has_no_calendar_fallback_and_preserves_true_lead`, `test_audit_signal_uses_session_baseline_and_full_price_sequence`, and `tests/test_prediction_sensitivity.py::test_next_session_return_is_computed_before_sparse_signal_join` use known gapped dates and exact returns. `test_directional_agreement_excludes_zero_signal_and_zero_return` prevents zero from becoming negative/down. | Provider price gaps or erroneous closes remain data-quality issues; sensitivity code fails loudly on duplicate dates and invalid closes. |
| Sensitivity report omits or selects a variant | **Covered** | `tests/test_prediction_sensitivity.py::test_reports_every_variant_without_preference_and_exact_metrics` checks all four variants, exact metrics, and no preferred variant. `::test_output_is_strict_json_and_contains_alignment_audit` checks reproducible strict JSON and row-level alignment metadata. | The report is in-sample/exploratory; no test can turn it into out-of-sample validation. |
| Duplicate price sessions make target ambiguous | **Covered in sensitivity module** | `test_duplicate_market_dates_fail_loudly` asserts rejection rather than implicit aggregation. | Other consumers rely on the database primary key to prevent duplicates and do not repeat this exact guard. |
| Thirty observations described as reliability | **Terminology reviewed; behavior indirectly tested** | Evaluate/chart tests cover preliminary/reporting behavior, and current docs consistently define the threshold as a display gate. | No repository-wide prose test enforces future wording. |

## Stage 4 inference and reproducibility risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Independent-headline inference understates dependence | **Covered for implemented methods** | `tests/test_polarization_inference.py::test_exact_descriptives_effect_size_and_cluster_bootstrap_are_deterministic` asserts exact raw gap/effect size and date-cluster bootstrap; `::test_cluster_and_rank_diagnostics_are_explicit` verifies few/invalid clusters and saturated designs are exposed. | Cluster methods address specified dependence only; few clusters, camp definitions, source selection, and scorer error remain. |
| Selection is conflated with framing | **Covered** | `::test_canonical_events_hold_event_fixed_and_selection_remains_separate` checks cross-camp/camp-only event coverage and within-event gaps as separate outputs. | The production bridge currently has no defensible repeated shared-event identity. |
| One-headline bridge or lexical reuse is called same-event evidence | **Covered** | `::test_bridge_ids_are_not_treated_as_shared_events_and_fallback_has_no_reuse` verifies bridge rejection, one-to-one matching, and sensitivity-only wording. | Lexical candidates require manual validation and are not verified events. |
| Canonical URL collapse erases outlet observation | **Covered** | `::test_database_loader_preserves_cross_source_shared_url_observations` proves source-distinct raw observations drive outlet counts while legacy canonical-only rows remain available. | Historical rows collected before the raw audit cannot be reconstructed. |
| Missing inference dependency erases descriptives | **Covered** | `::test_missing_statsmodels_is_reported_without_losing_descriptives` keeps raw evidence while reporting regression unavailable. | Dependency-version numerical differences remain possible and are surfaced diagnostically. |
| Public demo needs a key, model, network, or private DB | **Covered** | `tests/test_demo.py::test_offline_demo_writes_audited_deterministic_artifacts` blocks network/model initialization, uses committed samples, and asserts the table, audit classifications/checks, and chart. | The demo proves reproducibility of mechanics, not validity on the private/full sample. |

## Phase 0 production-reconciliation risks

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| A migration rewrites historical model output | **Covered** | `tests/test_production_migration.py::test_historical_scores_are_never_rewritten` hashes `sentiment_score`, `sentiment_label`, `scored_at` and `model_name` across a 3465-row production-shaped fixture and asserts the digest is constant through `init_db`, a repeat `init_db`, session backfill and relevance reconciliation. `::test_migration_is_purely_additive` proves no table or column is removed. | The digest covers scored fields; a defect confined to a column outside it would not be caught. |
| A repeated `init_db()` silently mutates data | **Covered** | `::test_second_init_db_is_a_no_op` compares a content digest computed per table in primary-key order, not a file hash, so SQLite page churn cannot mask a real change. | None for the tested tables. |
| A superseded trading calendar is trusted, or a correction passes silently | **Covered** | `::test_corrected_signal_dates_target_real_trading_sessions` requires every re-derived `signal_date` to be a trading day *and* to have an actual price row; `::test_calendar_correction_is_reported_as_a_deviation` requires the change to surface as a signed-off deviation rather than a silent pass. | Correctness is judged against stored prices; a session absent from the price table cannot be verified this way. |
| Missing publication hours are treated as tradable | **Covered** | `::test_missing_hour_rows_receive_a_conservative_bucket` asserts every unknown-hour row lands in `unknown` or `weekend_or_holiday` and that no row keeps a NULL bucket. | Conservative assignment defers the observation; it does not recover the true publication time. |
| A stale local database overwrites a newer canonical snapshot | **Covered** | `tests/test_snapshot_guard.py` covers refusal of the exact 2026-07-07-over-2026-07-31 case, refusal on a single regressed marker while others advance, tolerance of absent reference tables, override recording, and CLI exit codes. | The guard compares monotonic markers; a corrupted database that still advances every marker would pass. |
| Legacy experiment identity is guessed rather than evidenced | **Covered** | `tests/test_legacy_experiment_provenance.py` proves exactly the eligible rows are assigned, six distinct conflicting-evidence cases stay unassigned, a conflicting scorer keeps blocking aggregation, existing identities are never overwritten, and the audit record is complete. | Eligibility depends on `model_name` being trustworthy; a mislabelled historical model name would satisfy the rule. |
| A reconstructed identity is mistaken for original provenance | **Covered** | `::test_reconstruction_is_distinguishable_from_original_assignment` verifies a scoring-time assignment never appears in the reconstruction audit; `::test_audit_table_is_append_only` verifies triggers abort `UPDATE` and `DELETE`. | None for the tested table. |
| A provenance rollback discards newer identities | **Covered** | `::test_rollback_restores_null_only_for_rows_this_migration_changed` and `::test_rollback_skips_rows_reassigned_after_the_migration` prove foreign and later assignments survive; `::test_rollback_appends_rather_than_erasing_history` proves rollback appends rather than deletes. | None for the tested paths. |
| The mixed-experiment fix is asserted but never demonstrated | **Covered** | `::test_new_v1p3_headline_aggregates_with_reviewed_legacy_rows` runs `aggregate_step` with no override after the backfill, and `::test_without_the_backfill_a_new_headline_blocks_aggregation` proves the same scenario fails beforehand. | Demonstrated on a fixture; the production run itself remains unexecuted while the workflow is disabled. |

## Operational hardening risks (2026-08-06)

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| Intentionally excluded headlines mark every run degraded | **Covered** | `tests/test_operational_hardening.py::test_excluded_pending_rows_do_not_degrade_the_run` and `::test_run53_shaped_state_is_success_not_degraded` reproduce the 198-row production state and require `success`; `::test_eligible_pending_rows_do_degrade_the_run` proves the alarm still fires for genuinely unresolved rows. | A degraded status still depends on the exclusion rules themselves being correct; a filter that over-excludes would look healthy apart from the informational count. |
| A genuinely stuck headline is hidden by the eligibility split | **Covered** | `::test_eligible_retry_and_failed_rows_remain_visible` keeps eligible retry/failed rows degrading and separately counted; `::test_audit_detail_carries_every_required_key` pins the reported breakdown. | None for the tested states. |
| An excluded headline reaches an aggregate | **Covered** | `::test_excluded_rows_never_enter_aggregation` proves only the eligible headline reaches `daily_signal_variants`. | None for the tested path. |
| A repaired price bar silently loses its `corrected` provenance | **Covered** | `::test_corrected_survives_an_identical_settled_refresh` reproduces the run-53 regression; `::test_corrected_retained_when_a_later_refetch_changes_values` covers a materially changed refetch; a 13-case parametrized table pins every transition including the forbidden `corrected → provisional`. | Provenance already erased before the fix shipped had to be restored manually; the audit trail for that lives in the data-branch commit message, not the database. |
| A fetch-window boundary nulls a valid stored return | **Covered** | `::test_boundary_row_keeps_its_return_across_a_refresh`, `::test_new_row_uses_a_predecessor_outside_the_fetch_window`, and `::test_corrected_close_updates_its_own_and_the_following_return` verify chaining across stored history rather than the download window. | Returns depend on the stored close series being correct; a wrong close still yields a wrong return, flagged only by the review reasons. |
| An unsettled bar enters the return series | **Covered** | `::test_provisional_row_never_enters_the_return_series` requires a provisional bar to hold NULL and stay out of `get_prices(complete_only=True)`. | None for the tested path. |
| An after-close job runs mid-session | **Covered** | `::test_guard_blocks_before_settlement`, `::test_guard_allows_after_settlement`, `::test_guard_uses_the_half_day_close`, `::test_guard_converts_utc_to_istanbul`, and `::test_before_settlement_is_a_no_op` (which fails if any price fetch is attempted) exercise the Istanbul runtime guard. | The guard depends on the holiday/half-day calendar being current; a stale calendar would misjudge settlement. |
| The two workflows overwrite each other's snapshot | **Covered** | `::test_both_workflows_share_one_database_writer_concurrency_group` requires one shared group with `cancel-in-progress: false`. | GitHub concurrency is honoured per repository; a manual local run bypasses it entirely. |
| The after-close job gains scrape/scorer reach | **Covered** | `::test_after_close_workflow_never_scrapes_or_scores` asserts on injected env and executed commands rather than prose, so a comment cannot satisfy it; `::test_after_close_workflow_guards_before_publishing` and `::test_after_close_workflow_never_commits_readme_figures` pin ordering and figure safety. | Workflow tests verify configuration, not runner behaviour; a provider or runner change is only caught by an actual run. |

## Phase A descriptive-indicator risks (2026-08-06)

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| The family layer silently rewrites the frozen measurement | **Covered** | `tests/test_phase_a_taxonomy.py::test_classification_never_mutates_scores_categories_or_experiment` compares scores, labels, timestamps, model names, experiment ids and detailed categories before and after; `tests/test_phase_a_pipeline.py::test_indicators_do_not_touch_scores_categories_or_provenance` repeats it on a production-shaped database through the full cohort fingerprint. | The guarantee covers the fingerprinted fields; a defect confined to a column outside that set would not be caught. |
| A category has no family, or a family is guessed | **Covered** | Parametrized mapping tests over the full production category set; `::test_unmapped_category_is_other_and_flagged_ambiguous` and `::test_missing_category_is_reported_not_guessed` require the ambiguous flag and a review reason rather than a silent assignment. | Rules are Turkish string matching; a genuinely novel phrasing lands in `other` and is visible only through the coverage report. |
| The banking boundary collapses into an industry rule | **Covered** | Six sector fixtures and five named-issuer fixtures assert the entity-specificity split; `::test_named_bank_without_an_issuer_event_is_flagged_for_review` pins the deliberately ambiguous middle case. | The named-bank list is finite; an unlisted issuer is treated as sector news. |
| Market recap misses real recaps, or eats genuine news | **Covered** | Ten Turkish positive fixtures and twelve false-positive fixtures covering new indices, listings, appointments, regulatory decisions, disclosures and forward-looking commentary; `::test_recap_exemption_is_reported_as_such` requires the exemption path to stay visible. | Fixtures are hand-written; recall on unseen phrasing is unmeasured, and the corpus share is reported rather than validated. |
| An indicator uses information from the date it describes | **Covered** | `tests/test_phase_a_indicators.py::test_changing_the_future_cannot_change_a_past_indicator` mutates every observation dated t or later, adds new later sessions, and requires every earlier value to be identical; `::test_volume_shock_uses_prior_sessions_only` pins the prior mean of a spike session. | Tested for abnormal tone and volume; any future indicator must add its own leakage test. |
| An insufficient sample is reported as a substantive zero | **Covered** | `::test_single_headline_reports_null_dispersion_not_zero`, `::test_disagreement_requires_a_minimum_number_of_sources`, `::test_abnormal_tone_returns_null_below_minimum_history` and `::test_zero_variance_baseline_yields_null_z_not_infinity` all require NULL. | Thresholds are conventions, not derived from a power analysis. |
| Syndicated copies inflate independence | **Covered** | `::test_syndicated_copies_do_not_inflate_observation_count` and `::test_source_breadth_counts_outlets_not_headlines` separate headline count, event count and outlet breadth. | Event identity relies on the bridge id; without one, each headline counts as its own observation. |
| Phase A breaks the existing aggregate or the run | **Covered** | `tests/test_phase_a_pipeline.py::test_indicators_leave_the_overall_variant_table_unchanged` and `::test_indicator_failure_degrades_without_corrupting_aggregates` inject a failure and require every pre-existing row to survive. | Fail-soft covers exceptions inside the step; an interpreter crash is out of scope. |
| The price-only workflow starts running headline analytics | **Covered** | `::test_after_close_workflow_does_not_run_phase_a_headline_analytics` and `::test_after_close_refresh_never_calls_indicator_code` fail if the indicator path is reached. | Configuration and code paths are tested, not runner behaviour. |
| The regime report becomes non-deterministic or makes causal claims | **Covered** | `::test_regime_report_is_deterministic` compares two serialized builds; `::test_demo_makes_no_predictive_claim` forbids claim-shaped language outside the disclaimers. | Wording checks are lexical; they cannot detect an unsupported inference expressed in new words. |
| The demo needs a key, network, model or private database | **Covered** | `tests/test_phase_a_demo.py` blocks sockets during the run and asserts determinism, capability coverage, NULL visibility and a runtime bound. | The demo proves mechanics on a fixture, not validity on the real corpus. |

## Event-dataset risks (2026-08-06)

| Risk | Coverage | Regression evidence | Residual risk |
|---|---|---|---|
| A recurring headline chains into one long-running pseudo-event | **Covered** | `tests/test_event_dataset.py::test_a_daily_recurring_headline_does_not_chain_across_months` feeds fifteen consecutive daily headlines and requires no group to outlive its window. | The window is a convention; genuinely multi-day stories are split at its boundary. |
| Grouping depends on input order | **Covered** | `::test_grouping_is_deterministic_regardless_of_input_order` compares forward and reversed input. | Determinism is proven for ordering, not across dictionary revisions, which is why the algorithm version is stored. |
| Unrelated headlines merge | **Covered** | `::test_different_families_never_group` and `::test_distant_headlines_do_not_group` pin the family and time gates. | Lexical similarity cannot detect a shared topic expressed in different words; such cases stay separate singletons. |
| Similarity evidence is lost | **Covered** | `::test_similarity_evidence_is_retained` requires a score, rule and algorithm version on every mapping. | None for the tested path. |
| A group of one outlet is read as corroboration | **Covered** | `::test_single_source_group_is_flagged` and `::test_summary_reports_cross_source_dispersion_only_with_two_voices` require NULL dispersion for one voice. | None for the tested path. |
| Manual review silently rewrites the algorithm's output | **Covered** | `::test_manual_split_and_merge_are_appended_not_applied`, `::test_event_group_audit_is_append_only` and `::test_regrouping_preserves_manual_audit_history`. | None for the tested path. |
| A return is used that could not have been earned | **Covered** | `::test_pre_open_uses_same_session_open_to_close`, `::test_post_close_trades_the_session_that_can_react`, `::test_during_session_is_blocked_for_want_of_intraday_data` and a parametrized eligibility table. | Windows assume fills at the open or close; slippage and impact are a later phase's concern. |
| A provisional bar enters a return | **Covered** | `::test_provisional_bars_are_never_used` proves settled-only visibility. | None for the tested path. |
| A contemporaneous control is used as if tradable | **Covered** | `::test_control_sets_declare_tradability` and `::test_control_panel_materialises_lagged_values` separate the two kinds and pin the lag. | The schema labels the distinction; enforcement at the point of use belongs to the future protocol. |
| Residuals leak the future through a full-sample beta | **Covered** | `::test_residuals_use_a_rolling_prior_window_only` requires the estimation window to end strictly before the described date and NULL below the minimum. | Rolling windows are short at current coverage, so most lagged residuals are NULL. |
| A brief overstates what a group is | **Covered** | `::test_brief_never_claims_a_verified_event` requires candidate wording and forbids recommendation language outside disclaimers. | Lexical checks cannot detect an unsupported inference in new words. |
| The event step breaks the run | **Covered** | `::test_events_step_fails_soft` injects a failure and requires existing tables to survive. | Fail-soft covers exceptions inside the step. |

## Timing and walk-forward validation risks (2026-08-07)

`tests/test_timing_audit.py` (29 tests) and
`tests/test_walk_forward_validation.py` (34 tests). Every risk below is one
whose realisation would *improve* the reported numbers rather than raise an
error, so none of them can be caught by ordinary operation.

| Risk | Status | Evidence | Residual gap |
|---|---|---|---|
| `signal_date` is silently reinterpreted | **Covered** | `TestSignalDateSemantics` derives both hypotheses from the calendar and requires production-shaped rows to refute one. `::test_classifier_would_report_hypothesis_a_if_rows_said_so` proves the verdict is read off the data rather than hard-coded. | The audit reads stored rows; a future ingest that writes a different convention is caught only on the next audit run. |
| Post-close and weekend windows drift a session again | **Covered** | `::test_post_close_is_not_shifted_by_a_session` pins entry and exit to explicit dates, and `::test_audit_flags_a_shifted_window` injects a deliberately shifted builder and requires a FAIL. | Pinned to the daily-bar model; intraday windows would need their own pins. |
| A gap window is presented as tradable | **Covered** | `::test_gap_windows_are_never_tradable` across all three buckets. | The flag is enforced in `eligible_rows`; a direct SQL consumer can still ignore it. |
| An event's bucket and session come from different headlines | **Covered** | `::test_bucket_and_session_come_from_the_same_member` and `::test_last_member_governs_not_the_earliest`. | None for the tested path. |
| Events straddling sessions enter evaluation | **Covered** | `::test_members_spanning_sessions_are_flagged` and `::test_conflicted_events_are_blocked_from_primary_evaluation`. | Conflicted groups stay queryable descriptively by design. |
| Duplicated outcomes are counted as independent | **Covered** | `::test_duplicated_outcomes_are_reported_not_hidden` and `::test_conflicting_outcomes_on_one_session_are_an_error`. | Session-level rows still carry serial dependence across adjacent sessions; the embargo bounds it but does not remove it. |
| A test fold leaks into training | **Covered** | `TestFolds` pins chronology, disjointness and the embargo; `::test_a_sufficient_sample_actually_fits` re-checks every prediction against **its own** fold. | Expanding windows deliberately train on earlier test sessions; that is the design, not a leak. |
| Preprocessing is fitted on the test fold | **Covered** | `::test_standardiser_uses_only_the_training_fold` and `::test_transform_does_not_recentre_the_test_fold`. | Only the standardiser is checked; any future preprocessing step needs its own test. |
| A random split appears | **Covered** | `::test_no_random_splitting_anywhere_in_the_module` scans the module source for `train_test_split`, `shuffle`, `random.sample`, `rng.permutation`. | A source scan cannot catch a differently named helper. |
| A contemporaneous control reaches a tradable model | **Covered** | `::test_contemporaneous_controls_are_never_tradable` and `::test_no_feature_set_contains_a_contemporaneous_input`. | None for the tested path. |
| An underpowered specification is fitted and quoted | **Covered** | `::test_gate_refuses_and_names_the_binding_requirement` and `::test_blocked_specification_is_not_fitted`. | The gate governs the runner; a caller invoking the estimator directly bypasses it. |
| The frozen protocol moves silently | **Covered** | `::test_hash_is_stable_across_calls` and `::test_hash_ignores_provenance_but_covers_the_specification`. | The hash proves the specification is unchanged, not that it was well chosen. |
| A small sample declares success | **Covered** | `::test_reduced_geometry_cannot_declare_success`. | None for the tested path. |
| Uncertainty ignores repeated sessions | **Covered** | `::test_cluster_bootstrap_resamples_clusters_not_rows` and `::test_bootstrap_is_deterministic`. | A cluster bootstrap handles cross-sectional dependence, not serial dependence. |

## Risks intentionally remaining

| Risk | Current evidence | Required next work |
|---|---|---|
| Live-provider integration | Mocked unit tests cover contracts, not continuing provider compatibility. | Small monitored integration checks that do not make the deterministic suite network-dependent. |
| Out-of-sample predictive validity | Established as **null** under a frozen protocol on 49 sessions, with most specifications refused by the sample-size gate. | More sessions. The binding constraint is data, not method: the protocol is fixed and rerunning it on a longer corpus requires no new decisions. |
| Serial dependence between adjacent sessions | The one-session embargo bounds story overlap; the cluster bootstrap does not model autocorrelation. | A block bootstrap or HAC standard errors once the sample supports estimating the dependence. |

## Maintenance rule

Whenever behavior changes, update this map in the same logical change. Name the exact test protecting the risk and retain the residual gap. Historical findings and figures are not silently regenerated by tests or schema initialization; an explicit result-changing rerun must be documented separately.

The final `python evaluate.py` verification completed successfully but emitted the upstream statsmodels `FutureWarning` that `verbose` is deprecated. It is not produced by the warning-producing deterministic tests and did not change results, so it remains visible rather than being globally suppressed.

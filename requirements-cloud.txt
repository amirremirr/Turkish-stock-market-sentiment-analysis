# Slim dependency set for the GitHub Actions daily run.
# The LLM scoring backend (sentiment_llm.py) needs NO torch/transformers — the
# 1.1 GB XLM-R model never loads — so the cloud runner installs in ~30s.
# (For the local XLM-R fallback backend, use the full requirements.txt instead.)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
statsmodels>=0.14.0
matplotlib>=3.7.0
yfinance>=0.2.36
tqdm>=4.65.0
